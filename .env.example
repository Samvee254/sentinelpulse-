# Copy this to .env and fill in real values when deploying.
# Defaults to local SQLite if DATABASE_URL is unset -- fine for development.

# DATABASE_URL=postgresql://user:password@host:5432/sentinelpulse
